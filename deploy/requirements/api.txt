# ── Runtime deps for the FastAPI REST API only ─────────────────────────────────
# (excludes offline pipeline deps like Whisper/yt-dlp/OpenCV/Jupyter —
#  see requirements.txt for the full data-collection + training stack)

torch==2.1.2
sentence-transformers==2.7.0
faiss-cpu==1.8.0
rank-bm25==0.2.2
transformers==4.40.2
huggingface-hub==0.23.2

numpy==1.26.4
python-dotenv==1.0.1
requests==2.31.0

fastapi==0.111.0
uvicorn[standard]==0.29.0
